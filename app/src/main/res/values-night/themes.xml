<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UTInclusiveRestrooms" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/theme_blue_gray</item>
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor">@color/theme_blue_gray</item>
    </style>
</resources>